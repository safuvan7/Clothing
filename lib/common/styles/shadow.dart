import 'package:flutter/material.dart';

class ShadowStyle {
  static final ProductShadow = BoxShadow(
    color: Colors.grey,
    blurRadius: 50,
    spreadRadius: 7,
    offset: const Offset(0, 2)
  );
}